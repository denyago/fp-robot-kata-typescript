import { fpRobotKataTypescript } from './fp-robot-kata-typescript';

describe('fpRobotKataTypescript', () => {
  it('should work', () => {
    expect(fpRobotKataTypescript()).toEqual('fp-robot-kata-typescript');
  });
});
